# OneSniffer — справочник по проекту

Документ для контекста при разработке. Обновлять при существенных изменениях в коде.

## Назначение

**OneSniffer** — расширение конфигурации 1С:Предприятие 8 (тип **AddOn**, префикс `OneSniffer_`), аналог сниффера HTTP-трафика внутри платформы. Целевая аудитория: среды без стороннего ПО на рабочей станции, CI/сборочные линии, отладка интеграций.

**Важно:** логируются только вызовы, которые приложение **явно** перевело на API расширения. Автоматического перехвата всех HTTP-вызовов платформы нет.

- Платформа: **8.3.17+**
- Версия расширения: **1.0.0.1**
- Хранилище настроек логирования: `InformationRegister.OneSniffer_Настройки` (общие для ИБ, `ХранилищеЗначения`)
- Настройки редактора Monaco: `ХранилищеОбщихНастроек`, ключ объекта `"OneSniff"` (персональные)
- Зависимости от типовой конфигурации: **БСП** (`ОбщегоНазначения`, `ДлительныеОперации`, `Пользователи`), опционально **РаботаВМоделиСервиса** (разделитель сеанса в логах)

---

## Архитектура (кратко)

```
Прикладной код
    └── _HTTPСоединение._ВызватьHTTPМетод()     ← единственная точка перехвата исходящих
            ├── ЛогированиеТрафика (настройки, JSON, markdown, ЖР)
            └── РегистрыСведений.ЛогиТрафика      ← запись / UI
```

| Объект | Роль |
|--------|------|
| `CommonModule._HTTPСоединение` | Обёртка `ВызватьHTTPМетод` с логированием |
| `CommonModule._HTTPСервис` | Публичный API входящих HTTP-сервисов |
| `HTTPService.OneSniffer_Демо` | Демо `GET /hs/onesniffer/ping` |
| `CommonModule.ЛогированиеТрафика` | Настройки, сериализация, markdown, повтор запросов, импорт/экспорт |
| `CommonTemplate.OneSniffer_MonacoEditorSingle` | BinaryData single-page HTML Monaco (`editor.single.html`), сборка из `monaco-html/` |
| `CommonModule.OneSniffer_РедакторКода*` | Мост 1С ↔ Monaco: `ТекстМакетаРедактора` → веб HTML / URL ВХ; канал `V8Proxy` + `#V8_request` / `ПриНажатии` (см. `.cursor/rules/html-v8proxy-bridge.mdc`) |
| `CommonModule.ЛогированиеТрафикаПереопределяемый` | Точка расширения для имён событий (пустая) |
| `CommonModule.ЛогированиеТрафикаПовтИсп` | Закомментированный код переменных среды ОС |
| `InformationRegister.ЛогиТрафика` | Хранение логов + формы Список / Просмотр / Настройки / Авторизация |
| `InformationRegister.OneSniffer_Настройки` | Общие настройки логирования (одна запись `Ключ=Основная`) |
| `Enum.МетодHTTP` | GET, POST, PUT, PATCH, DELETE, HEAD |

---

## Что сделано (исходящий HTTP)

### Перехват и запись

- Замена вызова: `HTTPСоединение.ВызватьHTTPМетод(...)` → `_HTTPСоединение._ВызватьHTTPМетод(HTTPСоединение, Метод, Запрос, ИмяВыходногоФайла, ИмяСобытия)`.
- При **выключенном** логировании — прямой вызов платформы без лишней обработки.
- При включённом:
  - сбор метаданных запроса (`ЛогиТрафика.ЛогЗапроса`);
  - выполнение запроса;
  - запись в **РС** (`ЗаписыватьВРС`) и/или **ЖР** (`ЗаписыватьВЖР`, тело опционально `ЗаписыватьВЖРТело`);
  - при исключении: текст в тело ответа лога, запись в ЖР (ошибка), запись в РС **вне транзакции** если активна транзакция.

### Форматы тел

- JSON в двоичных данных.
- **Gzip** + JSON (распаковка при записи ответа, собственная реализация `РаспаковатьФайлGZip`).
- Усечение тел при записи в РС/ЖР — настройка `МаксимальныйРазмерТела` (КБ; `0` = без лимита): текст — префикс + маркер, бинарь — заглушка. Превью вкладки «Картинки» умеет скачивать URL из JSON (Unsplash `urls.*`).

### Настройки (форма «Настройки», работают)

| Параметр | Описание |
|----------|----------|
| `ВключеноЛогирование` | Главный выключатель |
| `ЗаписыватьВРС` | Регистр сведений `ЛогиТрафика` |
| `ЗаписыватьВЖР` | Журнал регистрации (успешные — Информация) |
| `ЗаписыватьВЖРТело` | Тело запроса/ответа в ЖР |
| `ЗаписыватьВРСТело` | Тело запроса/ответа в РС |
| `ПолучениеИсключенийВключено` | Импорт записей HTTP из ЖР в РС при открытии списка (ошибки и успешные) |
| `ПолучатьИсключенияФЗ` | То же в фоновом задании |
| `СрокХранения` | Очистка старых записей (дни) |
| `ДатаЧтенияЖР` | Курсор импорта из ЖР |
| `УсекатьДлинныйBase64` | Усечение длинных base64 в тексте для Monaco/UI |

### Исключения и ЖР

- Событие ЖР: `OneSniffer.ВызватьHTTPМетод`.
- В `Данные` пишется JSON структуры лога (`ДанныеДляЗаписиВЖурналРегистрации`).
- `ЗаписатьИсключенияВРС` / `ЗаписатьИсключенияВРСВФоне` — догрузка из ЖР в РС (все уровни события `OneSniffer.ВызватьHTTPМетод`).
- После произвольного/повторного запроса с формы списка — синхронный импорт из ЖР, если включён только ЖР (без записи в РС).
- Настройки логирования не дублируются в структуре лога — всегда `ЗагрузитьНастройкиЛогирования()`.
- Курсор импорта ЖР: настройка `ДатаЧтенияЖР` в РС `OneSniffer_Настройки`.

### UI инструмента (форма списка `ЛогиТрафика`)

- Список логов с отбором по периоду, визуальное различие **входящий / исходящий** (`Входящий` → `ИндексНаправления`).
- Просмотр: два Monaco-поля (запрос / ответ); заголовки в тексте; вкладка **Картинки** — только при наличии превью.
- Редактор «произвольного запроса» (markdown-подобный текст), **отправка повторно**, в т.ч. через `_ВызватьHTTPМетод`.
- Переменные в тексте запроса: `{{$guid}}`, `{{$time}}`, `{{Выражение}}`.
- Экспорт: файл markdown, **curl**, код 1С.
- Импорт логов из файла (`ЗагрузитьЛогиHTTPИзФайла`).
- Расшифровка **Bearer** в заголовке Authorization (просмотр).
- Форма **авторизации** для редактирования заголовка (безопасное хранение).
- Тестовый запрос, сохранение в файл, удаление из списка.

### Имена событий

- Параметр `ИмяСобытия` в `_ВызватьHTTPМетод`.
- Если пусто — `ЛогированиеТрафика.ИмяСобытия`: эвристики для URL Интернет-поддержки 1С, иначе «Произвольный запрос».
- Переопределение: `ЛогированиеТрафикаПереопределяемый.ИмяСобытия` (сейчас пусто).

### Протокол обмена (markdown)

Формат экспорта/импорта и повторных запросов:

```text
### ИмяСобытия [incoming]
GET https://host/path
Header: value

<body>

# HTTP/1.1 200 OK
	Header: value
	<body>
```

Маркер `[incoming]` поддерживается при **импорте** и **экспорте**; при **живом** входящем перехвате выставляется `Входящий = Истина` (API `_HTTPСервис`).

---

## Исходящий HTTP — статус, GitHub issues и план доработки

Цель блока: довести **исходящий HTTP** до состояния «можно закрыть направление» — стабильный перехват, предсказуемые настройки, документация, закрытие релевантных issues.

Репозиторий: [ViktorErmakov/OneSniffer](https://github.com/ViktorErmakov/OneSniffer/issues)

### Сводка по issues (только исходящий HTTP и смежное)

| # | Название | Статус | Отношение к исходящему HTTP |
|---|----------|--------|-----------------------------|
| [4](https://github.com/ViktorErmakov/OneSniffer/issues/4) | Потеря лога при исключении в `ВызватьHTTPМетод` | ✅ closed | ЖР + догрузка в РС, запись вне транзакции |
| [5](https://github.com/ViktorErmakov/OneSniffer/issues/5) | Настройки логирования в ЖР и РС | ✅ closed | `ЗаписыватьВЖР`, `ЗаписыватьВРС` |
| [6](https://github.com/ViktorErmakov/OneSniffer/issues/6) | Чтение исключений из ЖР в ФЗ | ✅ closed | `ПолучатьИсключенияФЗ` |
| [10](https://github.com/ViktorErmakov/OneSniffer/issues/10) | Записывать тело в ЖР отдельной настройкой | ✅ closed | `ЗаписыватьВЖРТело` |
| [13](https://github.com/ViktorErmakov/OneSniffer/issues/13) | Тормоза на больших двоичных данных в списке | ✅ closed | Усечение base64 `image` при **записи**; в UI при просмотре ограничений может не хватать |
| [16](https://github.com/ViktorErmakov/OneSniffer/issues/16) | Кнопка тестового запроса | ✅ closed | Демо без правок кода хоста |
| [9](https://github.com/ViktorErmakov/OneSniffer/issues/9) | Безопасное хранение Authorization, просмотр | ✅ closed (май 2026) | Маскирование в РС/ЖР, «глазок» на форме авторизации, Bearer decode |
| [11](https://github.com/ViktorErmakov/OneSniffer/issues/11) | Нет ошибок, если настройки ни разу не сохранялись | ✅ closed (май 2026) | `НормализоватьНастройкиЛогирования()` при загрузке |
| [12](https://github.com/ViktorErmakov/OneSniffer/issues/12) | Сохранение/загрузка лога в файл | ✅ closed (июн 2026) | Markdown import/export в README; env-автосохранение — фаза 2 |
| [14](https://github.com/ViktorErmakov/OneSniffer/issues/14) | Документация README и настроек | ✅ closed (июн 2026) | README + встроенная справка из README (`tools/build-docs.ps1`) |
| [18](https://github.com/ViktorErmakov/OneSniffer/issues/18) | «Писать тело» только вместе с ЖР | ✅ closed (май 2026) | `ЗаписыватьВРСТело` / `ЗаписыватьВЖРТело` разделены |
| [8](https://github.com/ViktorErmakov/OneSniffer/issues/8) | Monaco Editor на форме запроса | ✅ done | `monaco-html` + макет `OneSniffer_MonacoEditorSingle`, формы Список/Просмотр |
| [7](https://github.com/ViktorErmakov/OneSniffer/issues/7) | Демо-база | 🟡 open | Инфраструктура для тестов исходящих |
| [17](https://github.com/ViktorErmakov/OneSniffer/issues/17) | WSProxy / SOAP | 🟡 open | **Не HTTP**; отдельное направление после исходящего HTTP |

**Не относятся к исходящему HTTP:** [#15](https://github.com/ViktorErmakov/OneSniffer/issues/15) (обучение VA), [#17](https://github.com/ViktorErmakov/OneSniffer/issues/17) (SOAP).

### Фаза 1 исходящего HTTP — выполнено (май 2026)

Закрыты шаги **1.1–1.9**, технический долг **T1–T6**, issues **#9, #11, #18**; скрыты поля настроек фазы 2 (**2.5**).

Кратко по коду:

- **#18 / 1.1–1.2:** `ЗаписыватьВРСТело` и `ЗаписыватьВЖРТело` разделены; `_HTTPСоединение`, `ЗаписатьЛог`, форма «Настройки».
- **#11 / 1.6:** `НормализоватьНастройкиЛогирования()` при загрузке и сохранении; настройки не передаются и не хранятся в структуре лога.
- **#9 / 1.7:** маскирование `Authorization` / `Cookie` / `X-API-Key`; режим пароля и «Показать содержимое» на форме авторизации.
- **T1–T6 / 1.3–1.5, 1.8–1.9:** комментарий ЖР при успехе, `ИмяВыходногоФайла`, `Authorization`, цикл импорта ЖР, `Порт`.
- **ЖР → список:** импорт успешных и ошибочных записей; мгновенное появление в списке после произвольного/повторного запроса (режим «только ЖР»).

#### Настройки на форме без реализации (фаза 2)

Элементы скрыты в `Forms/Настройки/Module.bsl` (`УстановитьВидимостьДоступность`): трассировка B3, таймаут, повтор, env-файл.

#### Охват API платформы (исходящий)

Сейчас обёрнут **только** `HTTPСоединение.ВызватьHTTPМетод`. Не логируются вызовы:

- `Получить`, `ОтправитьДляОбработки`, `Записать`, `Изменить`, `Удалить` (если хост использует их напрямую).

**Предложение:** общий внутренний метод `ВыполнитьИсходящийHTTP(Соединение, Метод, Запрос, ...)` + тонкие экспортные обёртки `_Получить`, `_ОтправитьДляОбработки` и т.д., либо документировать, что хост должен сводить всё к `_ВызватьHTTPМетод`.

### Предложения сверх issues (имеет смысл для исходящего)

| # | Предложение | Зачем |
|---|-------------|-------|
| ~~P1~~ | ~~Лимит размера тела~~ | **Сделано** (2026-07-26): `МаксимальныйРазмерТела`, РС+ЖР |
| P2 | Маскирование `Authorization`, `Cookie`, `X-API-Key` в РС по умолчанию | ✅ #9 |
| P3 | Фильтр URL (маски / список хостов) — не логировать статику, health-check | Шум в CI |
| P4 | Запись **только заголовков** в РС при больших телах | Компромисс #18 / #10 |
| P5 | Регламентная очистка по `СрокХранения` (РЗ, не только кнопка) | Эксплуатация |
| P6 | Примеры в README: поиск вызовов в хосте, массовая замена, `ИмяСобытия` | #14, внедрение |
| P7 | Экспорт одного лога в `.http` / HAR (сейчас свой markdown) | Совместимость с IDE |
| ~~P8~~ | ~~Счётчик «незалогированных» вызовов~~ | **Отменено** (2026-07-26) — не делаем |

### План доработки (исходящий HTTP)

Оценки: **S** &lt; 1 дн., **M** 1–3 дн., **L** &gt; 3 дн.

**Фаза 1 выполнена** (шаги 1.1–1.9, T1–T6, #9, #11, #18, 2.5) — см. раздел выше.

**Этап 0 (июн 2026):** README, `tools/build-docs.ps1`, встроенная справка `Ext/Help/ru.html`, кнопка «Справка» на формах Список и Настройки — закрыты #14, #12 (markdown), пункт DoD по README.

#### Фаза 2 — настройки «доп. возможности» (форма уже есть)

| Шаг | Задача | Оценка |
|-----|--------|--------|
| 2.1 | Подключить `ВключенаТрассировка` (B3-заголовки) | S |
| 2.2 | Подключить `ТаймаутЗапросов` в повторе и при необходимости в обёртке | S |
| 2.3 | `ПовторПриОшибкеСоединения` (N попыток, задержка) | M |
| 2.4 | Опционально: запись markdown в файл по env (`ИмяПеременнойСреды...`) | M → закрывает #12 |

#### Фаза 3 — качество и эксплуатация

| Шаг | Задача | Issues | Оценка |
|-----|--------|--------|--------|
| 3.1 | ~~Лимит размера тела в РС + усечение в UI~~ | #13, P1 | **done** |
| 3.3 | Фильтр URL / хостов (опционально) | P3 | M |
| 3.4 | Регламентная очистка по `СрокХранения` | P5 | **done** |

#### Фаза 4 — документация и охват API

| Шаг | Задача | Issues | Оценка |
|-----|--------|--------|--------|
| 4.1 | README: все настройки, скриншоты формы, FAQ | #14 | M |
| 4.2 | Раздел «внедрение в хост»: поиск, замена, именование событий | #14, P6 | S |
| 4.3 | Обёртки `_Получить` / `_ОтправитьДляОбработки` / … **или** явный отказ в пользу одного метода | п.3 трекера | L (по решению) |
| 4.4 | Закрыть #12 с пометкой «реализовано markdown import/export» + опционально 2.4 | #12 | S |

#### Фаза 5 — по желанию (не блокирует «готово»)

- [#8](https://github.com/ViktorErmakov/OneSniffer/issues/8) Monaco Editor — **сделано** (single-page в `monaco-html/`, макет `OneSniffer_MonacoEditorSingle`, см. `documentation/monaco-editor-update.md`)
- [#7](https://github.com/ViktorErmakov/OneSniffer/issues/7) демо-база с примерами `_ВызватьHTTPМетод`  
- P7 экспорт HAR / `.http`  
- `ЛогированиеТрафикаПереопределяемый.ИмяСобытия` — пример в документации для хоста  

### Definition of Done — «исходящий HTTP готов»

- [x] Все вызовы через `_ВызватьHTTPМетод` стабильно пишут в РС/ЖР по настройкам без сюрпризов (#18).
- [x] Исключения и транзакции не теряют логи (#4).
- [x] Новый пользователь / пустые настройки — без ошибок (#11).
- [x] README описывает внедрение и настройки (#14); встроенная справка синхронизируется через `tools/build-docs.ps1`.
- [x] Issues #18, #11, #9 закрыты в коде и приняты на ИБ.
- [x] На форме нет «мертвых» переключателей без поведения (скрыты до фазы 2).
- [x] TODO T1–T6 закрыты.

---

## Что задумано, но не доделано

Актуальный трекер для поочерёдной отработки: [`docs/незавершённые-задумки.md`](docs/незавершённые-задумки.md). Ниже — краткий снимок (может отставать от трекера).

### 1. Входящий HTTP (главный пробел)

| Компонент | Статус |
|-----------|--------|
| Публичный API `_HTTPСервис` (`ЗаписатьВходящийЛог`) | Реализован |
| Менеджер РС — `НовыйЛог`, `ПодготовитьСодержание` | Реализован |
| Демо `HTTPСервис.OneSniffer_Демо` (`GET /hs/onesniffer/ping`) | Реализован |
| Кнопка «Демо входящий HTTP (IIS)» (текст в редактор + «Выполнить») | Реализована |
| Внедрение в HTTP-сервисы хоста | Opt-in: вызов `_HTTPСервис` в обработчиках хоста |

**Шаблон для хоста** — см. `README.md`, раздел «Входящий HTTP».

### 2. Настройки на форме без логики в модуле

Элементы есть в `Forms/Настройки/Form.form`, в `Module.bsl` закомментированы:

- `ВключенаТрассировка` — в коде заготовка заголовков **x-b3-traceid / x-b3-spanid** (`ManagerModule.bsl`, закомментировано).
- `ТаймаутЗапросов`
- `ПовторПриОшибкеСоединения`
- `ИмяПеременнойСредыДляСохраненияЛоговHTTP` (константа не создана в расширении)
- `ВключеныАвтоответы`, `ВключеныАвтосценарии` — **отменены и удалены** из кода (см. §3)

Связанный закомментированный код: `ЛогированиеТрафикаПовтИсп` (чтение env Linux/Windows), `ЗаписатьЛогЗапросаВФайл` в `ЗаписатьЛог`.

### 3. Автоответы

**Снято (2026-07-26):** функционал отменён; следы в коде удалены (парсер `autoanswer`, УО списка). См. [`docs/незавершённые-задумки.md`](docs/незавершённые-задумки.md) §1.

### 4. Прочие TODO в коде

| Место | Суть |
|-------|------|
| `ManagerModule.bsl:97` | `ИмяВыходногоФайла` при записи ответа — проверено (T1) |
| `ЛогированиеТрафика:915` | Обход бага платформы CR+LF при импорте файла |
| `ЛогированиеТрафикаПереопределяемый` | Пустой — задуман для кастомизации имён событий |

### 5. Authorization

- Запись в лог: каноническое имя `Authorization`; при чтении поддерживается устаревшее `Autorization`.

---

## Другие каналы трафика платформы (не покрыты)

Расширение **не перехватывает** без доработки:

| Механизм 1С | Комментарий |
|-------------|-------------|
| **HTTP-сервисы** (входящие REST) | Приоритет №1, см. выше |
| **FTP** (`FTPСоединение`) | Нет обёртки |
| **Почта** (`ИнтернетПочта`, POP3/SMTP/IMAP) | Нет |
| **Web-сервисы** (SOAP, `WSСсылки`) | Нет |
| **OData** | Через HTTP-сервисы / отдельные вызовы |
| **Внешнее соединение** / COM | Вне scope HTTP |
| Прямые вызовы `ВызватьHTTPМетод` без `_HTTPСоединение` | Не логируются |

Для «весь опциональный трафик» нужна матрица: тип канала × направление × точка внедрения (обёртка / подписка / ручной вызов API записи лога).

---

## API для прикладных разработчиков

### Обязательная замена (исходящий)

```bsl
Ответ = _HTTPСоединение._ВызватьHTTPМетод(
    HTTPСоединение, HTTPМетод, HTTPЗапрос, ИмяВыходногоФайла, ИмяСобытия);
```

### API входящего HTTP (реализовано)

Точка входа для прикладного кода и HTTP-сервисов — **`CommonModule._HTTPСервис`** (публичный). Запись в РС — через `РегистрыСведений.ЛогиТрафика`.

```bsl
ИмяСобытия = "МойСервис.Обработчик";
ВремяНачалаЗамера = ТекущаяУниверсальнаяДатаВМиллисекундах(); // необязательно
Попытка
    // … обработка …
    _HTTPСервис.ЗаписатьВходящийЛог(Запрос, ИмяСобытия, Ответ, , ВремяНачалаЗамера);
    Возврат Ответ;
Исключение
    _HTTPСервис.ЗаписатьВходящийЛог(Запрос, ИмяСобытия, , ИнформацияОбОшибке(), ВремяНачалаЗамера);
    ВызватьИсключение;
КонецПопытки;
```

### Переопределение

```bsl
// CommonModule.ЛогированиеТрафикаПереопределяемый
Процедура ИмяСобытия(ИмяСобытия, АдресРесурса) Экспорт
    // заполнить ИмяСобытия по URL/контексту конфигурации-хоста
КонецПроцедуры
```

---

## Структура записи лога (РС `ЛогиТрафика`)

**Измерения:** `НомерСеанса`, `ВремяНачала`, `Дата`, `Сервер`

**Ресурсы (основные):** `Метод`, `Адрес`, `Входящий`, `Длительность`, `Клиент`, `КодОтвета`, `Пользователь`, `Порт`, `РазмерОтвета`, `Событие`, `Содержание` (хранилище значений, сжатие 5), `ЭтоЗащищенноеСоединение`, `Разделитель` (облако)

**Длительность:** для исходящих — автоматически в `_ВызватьHTTPМетод`; для входящих — опционально через 5-й параметр `ВремяНачалаЗамера` в `ЗаписатьВходящийЛог`, иначе `0`.

**Содержание (структура в хранилище):** `ЗаголовкиЗапроса`, `ТелоЗапроса`, `ЗаголовкиОтвета`, `ТелоОтвета`

---

## Рекомендуемый roadmap (общий)

**Исходящий HTTP:** детальный план — см. раздел [«Исходящий HTTP — статус, GitHub issues и план доработки»](#исходящий-http--статус-github-issues-и-план-доработки) выше.

### Фаза A — входящий HTTP

1. Публичный API `_HTTPСервис.ЗаписатьВходящийЛог` (один вызов в конце); менеджер РС — `НовыйЛог`, `ПодготовитьСодержание`.
2. Документация + шаблон для модулей `HTTPСервис` конфигурации-хоста.
3. Опционально: общий модуль-базовый обработчик, если в хосте много сервисов.

### Фаза B — довести зачатки (частично перенесено в план исходящего HTTP, фазы 2–3)

1. ~~Автоответы~~ — **отменены, следы удалены** (см. [`docs/незавершённые-задумки.md`](docs/незавершённые-задумки.md) §1).

### Фаза C — расширение охвата

1. Обёртки или ручной API для FTP / почты / SOAP — по приоритету проекта.
2. Фильтры: маскирование паролей; ~~лимит размера тела~~ (done); чёрный список URL.
3. Экспорт в HAR / интеграция с внешними снифферами.

### Фаза D — эксплуатация

1. ~~Регламент очистки по `СрокХранения`~~ — реализовано (фаза 3.4): `OneSniffer_ОчисткаЛоговТрафика`, порционная `ОчиститьИсторию`, UI на форме настроек.
2. Права доступа к форме логов и настройкам.

---

## Файлы для быстрого поиска

| Задача | Файл |
|--------|------|
| Точка перехвата исходящих | `CommonModules/_HTTPСоединение/Module.bsl` |
| API входящих HTTP-сервисов | `CommonModules/_HTTPСервис/Module.bsl` |
| Демо HTTP-сервис | `HTTPServices/OneSniffer_Демо/Module.bsl` |
| Запись в РС | `InformationRegisters/ЛогиТрафика/ManagerModule.bsl` |
| Настройки | `CommonModules/ЛогированиеТрафика/Module.bsl` (область `НастройкиЛогирования`) |
| UI список / повтор запроса | `InformationRegisters/ЛогиТрафика/Forms/Список/Module.bsl` |
| Импорт/экспорт markdown | `ЛогированиеТрафика`: `ПолучитьТекстЛога`, `ЗагрузитьЛогиHTTPИзФайла`, `ПрочитатьПротоколыЗапросовИзСтроки` |
| Пользовательская документация | `README.md`, `tools/build-docs.ps1`, `InformationRegisters/ЛогиТрафика/Ext/Help/ru.html` |

---

## Ограничения (не забывать)

1. **Opt-in:** без замены вызовов в конфигурации-хосте логов не будет.
2. **Производительность:** запись тел и JSON в РС/ЖР на высокой нагрузке дорога; нужны лимиты.
3. **Безопасность:** в логах могут оказаться токены, Basic/Bearer (частично маскируется при экспорте `БезАвторизации`).
4. **Расширение AddOn:** нельзя подписаться на чужие модули без правок хоста; входящие HTTP-сервисы почти всегда требуют кода в основной конфигурации или ещё одного расширения с `&Перед` / `&После` на публикуемых методах (если платформа и политика проекта позволяют).

---

*Последний анализ кодовой базы и issues GitHub: май 2026.*

---

## UI формы логов — обзор и идеи улучшения

Форма: `InformationRegister.ЛогиТрафика.Form.Список` (+ `Просмотр`, `Настройки`, `ФормаАвторизации`).

### Как устроено сейчас

| Зона | Элементы | Заметки |
|------|----------|---------|
| Верх | Период, **Развернуть** (всегда), файл, настройки, единый **Выполнить**, **Редактировать** | Вне edit «Выполнить» берёт текст лога строки; в edit — Monaco. Инструменты правки — только в режиме редактирования |
| Список | Динамический список, автообновление **30 с**, конец списка | Поиск **отключён** (`searchStringLocation: None`) |
| Детали (`Содержание`) | Скрыты по умолчанию (`ПоказыватьДанные`) | Single-page Monaco (`OneSniffer_MonacoEditorSingle` → TEMP `*_single.html`); текст после `ДокументСформирован` |
| Запрос | Одно HTML-поле: METHOD/URL + заголовки + тело; edit по кнопке | `ПолучитьТекстЛога(..., Ложь)`, язык `rest` |
| Ответ | Вкладки: тело (HTML, RO) + **Картинки** (условно) | `ПолучитьТекстОтветаЛога`, язык `rest`; код 1С — форма Просмотр, `Язык=bsl` |
| Редактор | Правка идёт в поле `HTMLТелоЗапроса` (режим **Редактировать**); отдельной панели «произвольный запрос» нет | Postman-подобный markdown, curl/1С, тестовые запросы — в меню «Инструменты» на основной панели |
| Оформление | Цвета меток, красный текст 4xx/5xx, стрелки направления | — |

Сильные стороны: всё в одном окне, повтор запроса, экспорт, метки цветом, разбор JSON в дерево, генератор curl/1С.

Слабые стороны: детали тяжёлые на больших телах (#13); просмотр (`Просмотр`) из **Развернуть** — правка с кнопкой «Сохранить» обратно в редактор запроса; curl/код 1С — по-прежнему только чтение.

### Опыт, на который стоит опереться

| Продукт | Что взять |
|---------|-----------|
| **Chrome DevTools → Network** | Таблица + панель деталей; колонки Status, Type, Size, Time; фильтр по типу/ошибкам |
| **Postman / Insomnia / Bruno** | Слева история, справа Request \| Response; Raw / Pretty / Headers |
| **Fiddler / Charles** | Акцент на времени и размере (для 1С — колонки уже есть, не хватает сортировки по длительности) |
| **HTTP Toolkit** | Понятные «flows», копирование одним кликом |
| **mitmproxy Web** | Компактный список + раскрытие тела (идея lazy-load) |

Не копировать целиком: в 1С нет нативного waterfall и HTTP/2 frames.

### HTML вместо форм 1С?

| Подход | Плюсы | Минусы | Вердикт |
|--------|-------|--------|---------|
| **Оставить управляемые формы** | Права, РС, БСП, тонкий/толстый клиент, расширение | Слабый JSON/UI | Основа остаётся |
| **ПолеHTMLДокумента** (тело, JSON) | Подсветка через встроенный CSS/Prism, сворачивание узлов | Веб-клиент и мобильный — ограничения; безопасность скриптов | **Точечно** для просмотра тела |
| **Monaco / CodeMirror в HTML** ([#8](https://github.com/ViktorErmakov/OneSniffer/issues/8)) | Редактор запроса как в IDE | Толстый клиент надёжнее; нужен макет и обмен с 1С | **Для редактора запроса** |
| **Отдельная HTML-страница в браузере** | Полная свобода вёрстки | Вне сеанса 1С, сложнее авторизация | Экспорт / «Открыть в браузере» |
| **Полная замена формы на SPA** | Красиво | Два стека, дублирование логики, типичный overkill для AddOn | **Не рекомендуется** |

**Рекомендация:** гибрид — список, настройки, запись в РС на формах 1С; просмотр/редактирование тел и JSON — HTML-поле или Monaco там, где платформа позволяет; fallback — текущие поля + дерево значений.

### Предложения по улучшению (приоритет)

#### Быстрые (нативные формы, без HTML)

1. **Строка-сводка** над деталями: `POST https://host/path` · `200` · `1,2 с` · `45 КБ` · пользователь · сеанс.
2. **Включить поиск** по списку (событие, адрес, код).
3. **Предустановленные отборы**: только ошибки; длительность &gt; N; хост содержит…
4. **Lazy-load деталей**: загружать тело/JSON только при открытии вкладки «Тело» / «JSON» (снять тормоза #13).
5. **Разделить** URL (`ТекущийВызов`) и тело запроса (сейчас склеены).
6. **Режимы UI**: «Инспектор» (детали RO) / «Редактор» (`РежимРедактированияЗапроса`) — инструменты правки только в edit (уже частично сделано).
7. **Сгруппировать команды**: «Файл», «Повтор», «Код», «Метки» — выпадающие меню (частично уже есть).
8. **Копировать** URL / тело / заголовки — отдельные кнопки на вкладках.
9. **Подсказка по HTTP-коду** — расшифровка из `РасшифровкаКодаСостоянияHTTP` во всплывающей подсказке колонки «Ответ».

#### Средние

10. **Request | Response рядом** (горизонтально), как в DevTools — вместо двух вертикальных блоков.
11. **Вкладки тела**: Raw / Pretty (JSON format) / Tree — не дублировать «тело» и «JSON» в разных местах.
12. **Сравнение двух логов** (diff тел) — для регрессий API.
13. **Связка с повтором**: кнопка «Отправить снова» из деталей без открытия редактора.
14. **Индикатор логирования** в шапке: вкл/выкл, пишется в РС/ЖР.

#### С HTML / [#8](https://github.com/ViktorErmakov/OneSniffer/issues/8)

15. **Monaco** в `HTMLТелоЗапроса` / `HTMLТелоОтвета` / `Просмотр`: подсветка, folding, multi-cursor (частично уже есть).
16. **HTML-просмотр JSON** с Prism/highlight.js для ответа (readonly).
17. **«Открыть в браузере»** — сгенерировать одностраничный отчёт по выделенным логам (для отправки в чат).

#### Документация / UX ([#14](https://github.com/ViktorErmakov/OneSniffer/issues/14), [#15](https://github.com/ViktorErmakov/OneSniffer/issues/15))

18. Встроенная справка по форме (как YaXUnit) или VA-тур по кнопкам.
19. Пустое состояние списка: «Включите логирование» + ссылка на настройки + «Тестовый запрос».

### Макет (целевой, ориентир Postman-lite)

```text
┌─ Период │ Поиск │ [Ошибки] [Медленные] │ ⚙ Настройки │ ● Логирование ВКЛ ─┐
├─ Список логов (Method URL Status Time Size Event) ────────────────────────────┤
├─ POST https://api.example/v1/order  ·  200  ·  0.8s  ·  12 KB ───────────────┤
├─ Request ────────────────┬─ Response ────────────────────────────────────────┤
│ [Headers][Body][JSON]    │ [Headers][Body][JSON]                             │
│ (Monaco или HTML Pretty) │ (HTML Pretty, lazy load)                          │
├─ ▼ Редактор / Повтор (сворачиваемый) ─────────────────────────────────────────┤
│  ### Событие …  + Выполнить │ curl │ 1С                                         │
└────────────────────────────────────────────────────────────────────────────────┘
```

### Связь с issues

| Issue | Улучшение UI |
|-------|----------------|
| #8 | Monaco в редакторе |
| #13 | Lazy-load, truncate в списке («тело 2 МБ») |
| #9 | Маска Authorization в деталях + «глазок» |
| #14 | Справка по панели |
| #15 | VA-тур |
| #16 | Уже есть тестовый запрос — вынести в onboarding |

### Порядок внедрения UI (если браться)

1. Lazy-load + строка-сводка + поиск (нативно, максимум эффекта).
2. Режимы Инспектор/Редактор + копирование.
3. HTML/Monaco для тела и редактора ([#8](https://github.com/ViktorErmakov/OneSniffer/issues/8)).
4. Diff, браузерный отчёт — по запросу.
